node_modules


# ssh-keygen -t ed25519 -C "your_email@example.com" [Generate a new SSH key (if needed)]

# eval "$(ssh-agent -s)"    [Start the SSH agent and add your key]
# ssh-add ~/.ssh/id_ed25519

# cat ~/.ssh/id_ed25519.pub [Copy the public key to your clipboard]
# ssh -T git@github.com [Test the connection to GitHub]